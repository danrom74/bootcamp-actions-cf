name: Deploy to Server
on:
  push:
    branches:
      - main

jobs:
  primer_job:
    name: Primer Job
    runs-on: ubuntu-latest
    
    env:
      MY_VAR: "Danilo Romero"

    steps:
      - name: Hola Mundo
        run: echo "Hola primer Mundo $MY_VAR"

  segundo_job:
    if: ${{ github.ref == 'refs/heads/main' }} 
    name: Segundo Job 
    needs: primer_job
    runs-on: ubuntu-latest
    steps:
      - name: Hola Mundo
        run: echo "Hola segundo Mundo ${{ github.ref}} " 