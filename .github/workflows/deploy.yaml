name: Deploy to Server
on:
  push:
    branches:
      - main

jobs:
  primer_job:
    runs-on: ubuntu-latest
    steps:
      - name: Hola Mundo
        run: echo "Hola primer Mundo"

  segundo_job:
    if: false # Esta condición evita que el trabajo se ejecute  
    needs: primer_job
    runs-on: ubuntu-latest
    steps:
      - name: Hola Mundo
        run: echo "Hola segundo Mundo" 